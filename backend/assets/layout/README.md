This folder contains authoritative layout references used for deterministic
saree part extraction after flattening.

Each layout consists of:
- A visual PNG reference
- A JSON file defining relative bounding boxes (0–1)

These layouts are versioned and must not be modified in-place.
